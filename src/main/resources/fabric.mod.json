{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${mod_version}",
  "name": "${mod_name}",
  "description": "${mod_description}",
  "authors": [ "${authors}" ],
  "entrypoints": {
    "client": [
      {
        "adapter": "kotlin",
        "value": "com.theendercore.worldy.bounds.client.WorldlyBounds::init"
      }
    ]
  },
  "mixins": [
    {
      "config": "worldly_bounds.client.mixins.json",
      "environment": "client"
    }
  ],
  "accessWidener": "worldly_bounds.classtweaker",
  "depends": {
    "java": ">=${java}",
    "minecraft": "${version_range}",
    "fabricloader": "*",
    "fabric-api": "*",
    "fabric-language-kotlin": ">=${fabric_kotlin}",
    "fzzy_config": ">=${fzzy_config}"
  },
  "contact": {
    "homepage": "https://github.com/${github_repo}",
    "issues": "https://github.com/${github_repo}/issues",
    "sources": "https://github.com/${github_repo}",
    "discord": "${discord_invite}"
  },
  "license": "${license}",
  "icon": "assets/worldly_bounds/icon.png"
}